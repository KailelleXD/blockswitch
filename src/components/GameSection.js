import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class GameSection extends Component {
  render() {
    return (
      <View>
        <Text> GameSection.js </Text>
      </View>
    )
  }
}

const styles = StyleSheet.create({})
