import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class FooterSection extends Component {
  render() {
    return (
      <View>
        <Text> FooterSection.js </Text>
      </View>
    )
  }
}

const styles = StyleSheet.create({})
